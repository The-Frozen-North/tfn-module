#include "NW_I0_PLOT"
//:: Brent: May 2002: Disabled this option
int StartingConditional()
{   return FALSE;
/*
    int iHide = GetSkillRank(SKILL_HIDE, OBJECT_SELF);
    if (iHide > 0)
    {
        return CheckIntelligenceLow();
    }
    return FALSE;     */
}
