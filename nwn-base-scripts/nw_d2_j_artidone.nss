void main()
{
    // * SetLocalInt(GetModule(),"NW_G_ARTI_PLOT" + GetTag(OBJECT_SELF),2);
    // * Had to move this local setting into the actual giving of rewards
    // * because the player is capable of breaking his plot
}

